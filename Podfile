platform :ios, "6.0"
target 'LupinusHTTP', :exclusive => true do
  pod "NSDictionaryAsURLQuery"
end

target 'LupinusHTTPTests', :exclusive => true do
  pod "OHHTTPStubs"
  pod "XCTestCase-RunAsync"
  pod 'OCHamcrest', '~> 4.0'
end
